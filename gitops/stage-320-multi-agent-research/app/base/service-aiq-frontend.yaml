---
apiVersion: v1
kind: Service
metadata:
  name: aiq-frontend
  namespace: research-agents
  labels:
    project: aiq
    app: aiq-frontend
    component: frontend
  annotations:
    argocd.argoproj.io/sync-wave: '5'
spec:
  type: ClusterIP
  ports:
  - port: 3000
    protocol: TCP
    name: http
  selector:
    app: aiq-frontend
