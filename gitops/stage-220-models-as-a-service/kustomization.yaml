apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Kuadrant CR, MaaS gateway, and tier resources are added after live CRD
# verification per PLAN.md. The DSC modelsAsService enablement lives in
# stage-110's DSC overlay (CoP component patch components/maas).
resources:
  - rhcl/operator/base
