apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# nvidia-api-credentials Secret (data key "api-key") is created by
# deploy.sh from .env NVIDIA_API_KEY and never committed.
resources:
  - gpt-oss-120b-hosted.yaml
  - nemotron-nano-30b-hosted.yaml
  - nemotron-mini-4b-hosted.yaml
  - nemotron-super-120b-hosted.yaml
