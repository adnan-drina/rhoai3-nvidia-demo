# Pristine DataScienceCluster base: every component Removed. Enable
# components only via Kustomize components (instance/components/*) composed
# in instance/overlays/demo, following the Red Hat CoP gitops-catalog
# openshift-ai pattern. Do not edit this file to enable components.
# DSCInitialization is not authored: rhods-operator 3.4.2 auto-creates
# default-dsci (verified live 2026-07-07).
# Schema verified against live DSC v2 CRD (oc explain, cluster-48jqq).
apiVersion: datasciencecluster.opendatahub.io/v2
kind: DataScienceCluster
metadata:
  name: default-dsc
  annotations:
    argocd.argoproj.io/sync-wave: "5"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  components:
    aipipelines:
      managementState: Removed
    dashboard:
      managementState: Removed
    feastoperator:
      managementState: Removed
    kserve:
      managementState: Removed
    kueue:
      managementState: Removed
    llamastackoperator:
      managementState: Removed
    mlflowoperator:
      managementState: Removed
    modelregistry:
      managementState: Removed
    ray:
      managementState: Removed
    sparkoperator:
      managementState: Removed
    trainer:
      managementState: Removed
    trainingoperator:
      managementState: Removed
    trustyai:
      managementState: Removed
    workbenches:
      managementState: Removed
