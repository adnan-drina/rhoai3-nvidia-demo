# Stage 220: Models-as-a-Service is nested under the kserve component in
# RHOAI 3.4 (deploys maas-api, default tiers, token RBAC).
apiVersion: datasciencecluster.opendatahub.io/v2
kind: DataScienceCluster
metadata:
  name: default-dsc
spec:
  components:
    kserve:
      managementState: Managed
      modelsAsService:
        managementState: Managed
